program FunctionTest;

function myFunc(x : int, y : float) {
  print("Function called");
}

main {
  // error: expecting 2 parameters, got 1
  myFunc(10);
  // error: funciton not declared
  unknownFunc(5, 2);
}

end
